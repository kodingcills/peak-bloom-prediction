# Set to 'true' if CDS credentials unavailable
SEAS5_FALLBACK_MODE=false

# Number of parallel jobs (0 = auto)
N_JOBS=0

# Open-Meteo pacing and retry controls
OPENMETEO_DELAY_SECONDS=1.5
OPENMETEO_MAX_ATTEMPTS=3
OPENMETEO_429_MIN_SLEEP_SECONDS=30
OPENMETEO_429_MAX_SLEEP_SECONDS=180
